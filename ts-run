
# Get the current directory so we can create an absolute path to the bootstrap
# https://stackoverflow.com/a/1482133/169491
THIS_PACKAGE=$(dirname "$0")
BOOTSTRAP_PATH="$THIS_PACKAGE/bootstrap.js"

node -r $BOOTSTRAP_PATH $@